name: Claude AI Assistant

# AI 어시스턴트는 필요시에만 실행되도록 최적화
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

on:
  # 댓글 트리거 (댓글에 @claude가 포함된 경우만)
  issue_comment:
    types: [created]
  pull_request_review_comment:
    types: [created]

  # 자동 PR 리뷰 (첫 기여자만 + 특정 레이블)
  pull_request:
    types: [opened]  # synchronize 제거로 빈도 감소

permissions:
  contents: read
  pull-requests: write
  issues: write
  id-token: write

jobs:
  # 댓글 기반 상호작용
  claude-interactive:
    if: |
      (github.event_name == 'issue_comment' && contains(github.event.comment.body, '@claude')) ||
      (github.event_name == 'pull_request_review_comment' && contains(github.event.comment.body, '@claude'))
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Run Claude Interactive
        uses: anthropics/claude-code-action@beta
        with:
          claude_code_oauth_token: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}

  # 자동 PR 리뷰 (외부 기여자 전용)
  claude-auto-review:
    if: |
      github.event_name == 'pull_request' &&
      github.event.pull_request.author_association == 'FIRST_TIME_CONTRIBUTOR'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Auto Review
        uses: anthropics/claude-code-action@beta
        with:
          claude_code_oauth_token: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
          direct_prompt: |
            This is a first-time contribution. Please provide:
            - Welcoming feedback
            - Code quality review
            - Go best practices suggestions
            - Security considerations
            - Performance tips
          use_sticky_comment: true
